import SwiftUI

extension Color {
    static let accentPrimary = Color(.systemPink)
    static let accentSecondary = Color(.systemRed)
    static let statusAvailable = Color(.systemGreen)
    static let statusReserved = Color(.systemOrange)
    static let statusSold = Color(.systemGray)
    static let statusInfo = Color(.systemBlue)
}
